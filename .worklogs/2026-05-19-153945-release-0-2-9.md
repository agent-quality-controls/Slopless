# Summary

Prepared the `0.2.9` release after the maximal widening pass. Bumped package metadata, added the release plan, and adjusted spelling config so local validation passes with the repository resume helper present.

# Decisions Made

- Used `npm version 0.2.9 --no-git-tag-version` so the release version bump can be committed with a worklog before tagging.
- Added `pipefail` to `cspell.config.json` because `npm run validate` scans the local `resume` helper.
- Kept the `resume` helper untracked because it existed before the release work and is not part of the npm package files.

# Key Files For Context

- `.plans/2026-05-19-153837-release-0-2-9.md`
- `package.json`
- `package-lock.json`
- `cspell.config.json`

# Verification

- `npm run validate`

# Next Steps

- Push `development`, open the release PR, wait for CI, merge to `main`, publish to npm, then tag `v0.2.9`.
