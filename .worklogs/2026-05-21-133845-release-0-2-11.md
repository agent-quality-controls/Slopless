Summary:
Prepared the `0.2.11` release from `main` after merging PR 43.

Decisions made:
- Bumped the npm package patch version because `0.2.10` is already published.
- Kept the release on Node 22+ runtime metadata.
- Published from the clean `origin/main` worktree rather than the divergent local `main` branch.

Key files for context:
- `package.json`
- `README.md`
- `skills/slopless/SKILL.md`

Next steps:
- Run validation.
- Commit and push the version bump to `main`.
- Publish `slopless@0.2.11` to npm and verify install/use from a temporary project.
