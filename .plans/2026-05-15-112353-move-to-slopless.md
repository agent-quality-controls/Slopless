# Goal

Move this repository from `~/Projects/websmasher/prosesmasher` to `~/Projects/agent-quality-controls/slopless`, create `agent-quality-controls/slopless` on GitHub, set it as `origin`, push the current work, and leave a resume helper in the moved repo.

# Approach

1. Commit the current repo state before changing remotes so the GitHub push includes the fixture3 migration and fixture corpus work.
2. Exclude transient fixture3 received output from version control because approved outputs live under `behavior/golden`.
3. Move the working tree to `~/Projects/agent-quality-controls/slopless`.
4. Create the GitHub repository `agent-quality-controls/slopless`.
5. Replace `origin` with the new GitHub repo URL and push `main`.
6. Add `code-sessions` in the moved repo root using `CODEX_THREAD_ID=019e1c9b-4630-7550-b32e-88d74af37345`.
7. Leave the old path as a small directory with only the resume helper needed to restart this session from the moved repo.

# Key Decisions

- Use the existing branch name `main`.
- Do not commit `.fixture3/`; it is received/diff runtime output.
- Keep `behavior/golden/` committed; it is the approved fixture3 source of truth.
- Use the GitHub organization slug `agent-quality-controls`.

# Files To Modify

- `.gitignore`
- `.worklogs/<timestamp>-move-to-slopless.md`
- `code-sessions`
